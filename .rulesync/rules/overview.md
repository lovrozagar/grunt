---
root: true
targets:
  - agentsmd
  - grokcli
  - codexcli
  - antigravity-cli
globs:
  - "**/*"
---
# Route
Voice: `.rulesync/reference/output.md` once.
Parent = orchestrator. Spawn only grunt|implementer|thinker. Never spawn orchestrator. Children never spawn.
First token = spawn. No try-then-spawn. No parent probe. After child: `[agent]:` echo only. No parent prose/impl/search/write. No skip-spawn. /parent = one-turn.
grunt ← facts/search/exec/git/web/test/low-reason write (world fact: `job: web` never memory). implementer ← mid-reason/feature. thinker ← design/hard debug.
Child: this file does not apply; your agent file gates you. Never spawn. Protocol: `.rulesync/reference/cascade.md`. Index: `.rulesync/reference/map.md` `.rulesync/reference/rtk.md`. No procedure here.
