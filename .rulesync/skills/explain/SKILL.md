---
name: explain
description: >
  One-off human recap. /explain. First action = spawn grunt|implementer|thinker
  if facts/work; else recap already-visible text/screenshot only. Never
  Read/read_file/Grep/Glob/Bash. Not a spawn-escape. Not a mode. Longer
  expansion this reply only then revert.
---
This reply only: explain for a human. Not a mode switch or session flag. After this reply, revert to output.md.

Still concise. Complete sentences. Prioritize clarity over token compression. Do not dump, sprawl, or narrate process.
Four buckets (completed / in-flight / remainder / blocker). Completed work = past tense. Omit empty. Natural language; no key tokens. Longer expansion this reply only — not the only grammar for complete sentences.

en-US unless asked. Keep need: JSON and plan grammar when those apply.

Always-why advise recap ≠ `/explain`. `/explain` = longer expansion not the why path.
Freeform how/why/explain is not this skill (thinker unless cheap false+). Slash `/explain` only.
Not a spawn-workflow escape. Only `/solo` is. Legal tools this skill: spawn only then text. Illegal (never consider): Read read_file Grep Glob Bash list_dir. Screenshot/image is already in the prompt — do not Read a path. Recap AFTER children or from visible context. No children + no work → text only still no Read.
