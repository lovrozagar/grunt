name = "orchestrator"
description = "Session agent. Do the work. Spawn grunt only under the hood when isolation is cheaper."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "danger-full-access"
approval_policy = "never"
developer_instructions = "Follow AGENTS.md. Do the work. Concise complete sentences with natural grammar. Scratch in `.tmp/grunt/`. Browse with `node scripts/browser.mjs`; the rail swaps to Chromium when Lightpanda is blocked. App e2e uses Playwright."
