name = "grunt"
description = "Isolation worker. Facts only."
model = "gpt-5.4-mini"
model_reasoning_effort = "low"
sandbox_mode = "danger-full-access"
approval_policy = "never"
developer_instructions = "Return isolation facts (≤8 lines, stash receipt when needed). A browse snap is the payload."
